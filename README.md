# Good Commit

A CLI tool to make git commits following conventional commits

# Usage

Currently, approval is pending on Chocolatey (and this project does not fit the criteria to make a request for extras) so the only way you can try is by downloading the exe and trying it on the terminal. 

